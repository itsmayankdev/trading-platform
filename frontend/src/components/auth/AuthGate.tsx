"use client";
import { useEffect,useRef,useState } from "react";
import { usePathname,useRouter } from "next/navigation";
const session=()=>fetch("/api/backend/api/v1/auth/session",{credentials:"include",cache:"no-store"});
const telemetry=(event_type:string,module:string,path:string,duration_ms=0)=>fetch("/api/backend/api/v1/telemetry/event",{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({event_type,module,path,duration_ms}),keepalive:true}).catch(()=>{});
const moduleFor=(path:string)=>path.startsWith("/scanner")?"scanner":path.startsWith("/alerts")?"alerts":path.startsWith("/replay")?"replay":path.startsWith("/evaluation")?"evaluation":path.startsWith("/validation")?"validation":path.startsWith("/evidence")?"pattern_quality":path.startsWith("/favorites")?"favorites":path.startsWith("/dashboard")?"market_memory":"market_memory";
export default function AuthGate({children}:{children:React.ReactNode}){const pathname=usePathname();const router=useRouter();const [ready,setReady]=useState(false);const started=useRef(0);const isPublic=pathname==="/"||pathname==="/login"||pathname.startsWith("/login/")||pathname==="/admin"||pathname.startsWith("/admin/");useEffect(()=>{started.current=Date.now();if(pathname==="/admin"||pathname.startsWith("/admin/")||pathname==="/login"||pathname.startsWith("/login/")){setReady(true);return}let alive=true;const check=async()=>{try{const r=await session();if(!alive)return;if(r.ok){const b=await r.json();if(b.authenticated){if(pathname==="/"){router.replace("/dashboard");return}setReady(true);return}}if(pathname!=="/")router.replace(`/login?next=${encodeURIComponent(pathname)}`);else setReady(true)}catch{if(pathname!=="/")router.replace(`/login?next=${encodeURIComponent(pathname)}`);else setReady(true)}};void check();const id=window.setInterval(()=>{void check()},5000);return()=>{alive=false;window.clearInterval(id);const duration=started.current?Date.now()-started.current:0;if(duration>1000&&!isPublic)void telemetry("page_time",moduleFor(pathname),pathname,duration)}},[pathname,router,isPublic]);useEffect(()=>{if(!ready||isPublic)return;void telemetry("page_view",moduleFor(pathname),pathname)},[ready,pathname,isPublic]);if(!ready&&!isPublic)return <div className="min-h-screen bg-[#070a0f]"/>;return <>{children}</>}
